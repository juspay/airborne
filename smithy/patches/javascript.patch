diff --git a/airborne_server_clients/javascript/sdk/package.json b/airborne_server_clients/javascript/sdk/package.json
index e2148fd0..e5306d95 100644
--- a/airborne_server_clients/javascript/sdk/package.json
+++ b/airborne_server_clients/javascript/sdk/package.json
@@ -3,13 +3,11 @@
     "description": "airborne-server-sdk client",
     "version": "0.18.1",
     "scripts": {
-        "build": "concurrently 'yarn:build:cjs' 'yarn:build:es' 'yarn:build:types'",
+        "build": "npm run build:types && npm run build:cjs && npm run build:es",
         "build:cjs": "tsc -p tsconfig.cjs.json",
         "build:es": "tsc -p tsconfig.es.json",
         "build:types": "tsc -p tsconfig.types.json",
-        "build:types:downlevel": "downlevel-dts dist-types dist-types/ts3.4",
-        "clean": "rimraf ./dist-* && rimraf *.tsbuildinfo || exit 0",
-        "prepack": "yarn run clean && yarn run build"
+        "build:types:downlevel": "downlevel-dts dist-types dist-types/ts3.4"
     },
     "main": "./dist-cjs/index.js",
     "types": "./dist-types/index.d.ts",
